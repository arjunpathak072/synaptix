You are a code analyst. Given this Python file, respond with ONLY a short label (max 6 words) describing its role, e.g. 'API route handler' or 'Database utilities'. No explanation, no thinking, just the label.

File: {path}

```python
{code}
```
